# Ignore all python binary files
*.pyc

# The server config file that's actually used in the apps
server_conf.json

# Extra Mac directory files
.DS_Store

# Some sort of app-generated files in Sam's system
*.swp

# This is the name I give CMake build/binary directories
build*

# A type of file QtCreator makes from CMake files
*.user

# Directory & files pyinstaller creates
# Bit of a conflict if we want to store bower installed packages
# because they often come in dist directories...
IPCA/dist
*.spec

# Matlab temp files
*.m~

# Bower js installs
bower_components
